$ docker run -it --network some-network --rm mongo mongo --host some-mongo test

$ docker run -it --network some-network --rm mongo mongo --host some-mongo test

docker run -p 8081:8081 --name todo-mongo -d mongo:latest